# Supabase Database Connection
# Supabaseプロジェクトの「Settings」「Database」から接続文字列を取得
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@[PROJECT-REF].supabase.co:5432/postgres?pgbouncer=true"

# JWT Secret (以下のコマンドで生成: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET="your-secret-key-here-change-in-production-use-long-random-string"

# Server Port
PORT=5000
